# 0) No need to follow those rules that have a '#' in front of them

1) Don't run any build or also deploy command; just tell me. I will run those commands.

# 2) Don't use Git because I don't use Git.

# 3) Don't run any command; this is a server that is connected with sshfs, not any type of SSH, so even if you run any command, it will not reach the server's terminal. The command will execute in my desktop terminal so running commands is meaningless and just a waste of resources

# 4) but you can easily use find, cp, mv, rm, ls, cat commands


# 5) don't open browser for any reason, not even for testing or by mistake.
